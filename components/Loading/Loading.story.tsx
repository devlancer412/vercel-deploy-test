import { addDecorator } from '@storybook/react'
import React from 'react'
import { Loading } from './'

export default {
  component: Loading,
  title: 'Loading',
}

export const loading = () => <Loading />
